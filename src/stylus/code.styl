pre, pre.hljs
    position relative
    overflow auto
    max-height 15em
    padding 1.4em
    margin 1.4em -1.4em
    border-radius br
    border 1px solid bl
    border-color var(--border-light)
    background white
    background var(--panel-light)

.aCopyCodeButton
    position absolute
    right 0
    top 0
    border-radius 0 br 0 br
    border 1px solid bl
    border-right 0
    border-top 0
    border-color var(--border-light)